#ifndef ENDGAME_SOLVER_H
#define ENDGAME_SOLVER_H

#include "core/types.h"

// Endgame solver stub. Returns 0 (unknown/not implemented).
int solveEndgame(const Position& pos);

#endif // ENDGAME_SOLVER_H
