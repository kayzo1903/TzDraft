import { JoinQueueUseCase } from './join-queue.use-case';
import { GameStatus, GameType } from '../../shared/constants/game.constants';

describe('JoinQueueUseCase Clean', () => {
  it('should be defined', () => {
    expect(true).toBe(true);
  });
});
