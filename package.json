{
  "name": "tzdraft",
  "version": "0.1.1",
  "private": true,
  "description": "Tanzania Drafti - A modern online drafts game platform",
  "license": "UNLICENSED",
  "workspaces": [
    "backend",
    "frontend",
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r run start:dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint"
  },
  "pnpm": {
    "overrides": {
      "react": "19.1.0",
      "react-dom": "19.1.0",
      "expo-constants": "~18.0.13"
    }
  }
}
